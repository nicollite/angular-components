import { AngHint } from "./hint.directive";

describe("HintDirective", () => {
  it("should create an instance", () => {
    const directive = new AngHint();
    expect(directive).toBeTruthy();
  });
});
