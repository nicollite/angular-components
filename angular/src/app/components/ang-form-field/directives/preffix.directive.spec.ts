import { AngPreffix } from "./preffix.directive";

describe("PreffixDirective", () => {
  it("should create an instance", () => {
    const directive = new AngPreffix();
    expect(directive).toBeTruthy();
  });
});
