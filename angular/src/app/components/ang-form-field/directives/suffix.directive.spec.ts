import { AngSuffix } from "./suffix.directive";

describe("SuffixDirective", () => {
  it("should create an instance", () => {
    const directive = new AngSuffix();
    expect(directive).toBeTruthy();
  });
});
