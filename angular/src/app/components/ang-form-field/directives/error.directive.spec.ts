import { AngError } from "./error.directive";

describe("AngErrorDirective", () => {
  it("should create an instance", () => {
    const directive = new AngError();
    expect(directive).toBeTruthy();
  });
});
